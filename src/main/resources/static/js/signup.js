const f=document.getElementById('signupForm');
f.addEventListener('submit',async e=>{e.preventDefault();const m=document.getElementById('msg'),b=f.querySelector('button');if(password.value!==confirm.value){m.textContent='Passwords do not match.';return}b.disabled=true;m.textContent='Creating your account…';try{const r=await fetch('/api/auth/signup',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:name.value,email:email.value,password:password.value})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Could not create account.');m.textContent=d.message||'Account created.';setTimeout(()=>location.href='/login.html',800)}catch(x){m.textContent=x.message}finally{b.disabled=false}});
