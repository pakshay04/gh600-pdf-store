async function csrf(){const r=await fetch('/admin/csrf',{credentials:'same-origin'});if(!r.ok)throw new Error('Unable to initialize secure sign in.');return (await r.json()).token;}
const f=document.getElementById('loginForm');
if(f)f.addEventListener('submit',async e=>{e.preventDefault();const b=f.querySelector('button');const m=document.getElementById('msg');b.disabled=true;m.textContent='Signing in…';try{const token=await csrf();const body=new URLSearchParams(new FormData(f));const r=await fetch('/login',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/x-www-form-urlencoded','X-XSRF-TOKEN':token},body});if(r.redirected){location.href=r.url;return}m.textContent='Invalid email or password.';}catch(x){m.textContent=x.message}finally{b.disabled=false}});
(async()=>{try{const d=await (await fetch('/api/auth/config')).json();document.querySelectorAll('.google-btn').forEach(g=>{if(!d.googleEnabled){g.style.display='none';const divider=g.previousElementSibling;if(divider?.classList.contains('auth-divider'))divider.style.display='none';}})}catch(e){}})();
